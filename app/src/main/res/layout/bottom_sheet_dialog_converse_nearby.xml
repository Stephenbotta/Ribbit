<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="view"
            type="com.pulse.ui.conversenearby.post.PostNearByActivity" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvPublicly"
            android:layout_width="0dp"
            android:layout_height="?attr/actionBarSize"
            android:drawableStart="@drawable/ic_public"
            android:drawablePadding="16dp"
            android:fontFamily="@font/roboto_text_regular"
            android:gravity="start|center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="@string/dialog_post_label_publicly"
            android:textColor="@color/textGray"
            android:textSize="18sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/divider_1"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="@color/greyImageBackground"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvPublicly" />

        <TextView
            android:id="@+id/tvFollowers"
            android:layout_width="0dp"
            android:layout_height="?attr/actionBarSize"
            android:drawableStart="@drawable/ic_group"
            android:drawablePadding="16dp"
            android:fontFamily="@font/roboto_text_regular"
            android:gravity="start|center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="@string/dialog_post_label_follower_selected_people"
            android:textColor="@color/textGray"
            android:textSize="18sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/divider_1" />

    </android.support.constraint.ConstraintLayout>

</layout>
